---
name: mx-berk-pipeline
display_name: 选股→投顾 每日管线
title: mx-xuangu + ai-berkshire 选股投顾管线
description: >
  每日自动/手动触发的选股→投顾管线。多策略并行扫描（动量+价值+高股息），
  行业分散化约束，Berkshire 六关快筛，输出结构化操作建议。30 天模拟跟踪。
  v2.1：策略C加ROE>15%+市值>200亿降噪；mx-data串行+重试防NoneType；CSV数据复用减少API调用。
version: 2.1.0
author: WorkBuddy
---

# mx-berk-pipeline v2：选股→投顾 每日管线

## 定位

```
多策略扫描(mx-xuangu) → 去重+行业约束 → 财务数据(mx-data) → Berkshire六关快筛 → 操作建议
```

自动定时触发（11:30 / 13:30 / 14:20），输出结构化建议，用于 30 天模拟跟踪验证。

## 前置条件

- `MX_APIKEY` 环境变量已设置（东方财富妙想 API Key）
- `ai-berkshire` 仓库已克隆到 `D:\claudecode\.workbuddy\2026-06-03-19-30-44\ai-berkshire`
- Python 3.12+ 可用，已安装 pandas、openpyxl、requests
- WSL 可用（用于运行 ai-berkshire 的 financial_rigor.py 工具）

## 执行流程

### Phase 1：多策略并行扫描（mx-xuangu）

**核心改进：同时运行 3 组互补策略，取并集，避免单一视角盲区。**

每组策略用一个 mx-xuangu 查询，结果合并去重。3 组查询**必须并行执行**（用 3 个独立的 Bash 调用）。

#### 策略 A：动量稳健型（捕捉资金流入 + 估值合理）

```
"今日涨幅大于1% 小于7% 成交量大于2亿 市盈率小于30 市盈率大于0 A股"
```

- 涨幅 1%-7%：捕捉有资金流入但未暴涨的标的（排除涨停/接近涨停的追高风险）
- 成交量>2亿：保证流动性
- PE 在 0-30：排除亏损股和高估值

#### 策略 B：价值质量型（低估值 + 高盈利）

```
"市盈率小于20 市盈率大于0 市净率小于4 ROE大于12% 涨幅大于-1% A股"
```

- PE<20 + PB<4：估值便宜
- ROE>12%：盈利能力强
- 涨幅>-1%：当日未大跌（排除正在崩盘的）

#### 策略 C：高股息防御型（现金流好 + 分红稳定）

```
"股息率大于3% 市盈率小于25 ROE大于15% 连续三年分红 市值大于200亿 A股"
```

- 股息率>3%：有真实现金回报
- ROE>15%：过滤低盈利质量的高股息股（v2.1 优化：原无 ROE 约束导致 200 只噪音）
- 连续三年分红：分红稳定
- 市值>200亿：排除中盘投机（v2.1 优化：原>100亿门槛太低，返回过多）

**执行命令（并行）：**
```bash
cd "C:\Users\69050\.workbuddy\skills\mx-xuangu" && python mx_xuangu.py "今日涨幅大于1% 小于7% 成交量大于2亿 市盈率小于30 市盈率大于0 A股"
cd "C:\Users\69050\.workbuddy\skills\mx-xuangu" && python mx_xuangu.py "市盈率小于20 市盈率大于0 市净率小于4 ROE大于12% 涨幅大于-1% A股"
cd "C:\Users\69050\.workbuddy\skills\mx-xuangu" && python mx_xuangu.py "股息率大于3% 市盈率小于25 ROE大于15% 连续三年分红 市值大于200亿 A股"
```

**Phase 1 输出：** 3 组候选列表，合并后通常 10-30 只。

### Phase 1.5：去重 + 行业分散化约束

**核心改进：强制行业分散化，避免持仓过度集中。**

1. **去重**：按股票代码去重，同一股票出现在多组策略中只保留一次（标记来源策略）
2. **行业归类**：根据股票名称/代码判断所属行业（金融、能源、消费、科技、医药、材料、制造等）
3. **行业上限**：同一行业最多保留 2 只候选
4. **排序优先级**：
   - 多策略命中的优先（被 2-3 个策略同时选中 = 更可靠）
   - 同策略内按市值降序（大盘优先）
5. **最终候选上限**：取前 8-10 只进入 Phase 2

**行业判断规则（基于股票名称关键词）：**
- 银行/证券/保险 → 金融
- 煤炭/石油/电力/燃气 → 能源
- 食品/饮料/白酒/乳业 → 消费
- 半导体/芯片/软件/互联网 → 科技
- 医药/生物/医疗 → 医药
- 矿业/钢铁/有色/化工 → 材料
- 汽车/家电/机械 → 制造
- 其他 → 综合

### Phase 1.7：CSV 数据复用（减少 API 调用）

mx-xuangu 的 CSV 输出已包含每只股票的 PE、PB、ROE、股息率、涨跌幅等字段。在进入 Phase 2 之前，先从 CSV 中提取这些数据，避免重复查询。

**操作**：
1. 读取 3 个策略的 CSV 文件（路径：`C:\root\.openclaw\workspace\mx_data\output\`）
2. 用 CSV 中的 PE/PB/ROE 做第一轮粗筛（PE<30、PB<5、ROE>10）
3. 只对粗筛后的候选调用 mx-data 查年报和行情

**CSV 输出路径**：mx-xuangu 输出到容器路径 `\root\.openclaw\workspace\mx_data\output\`，对应 Windows 路径 `C:\root\.openclaw\workspace\mx_data\output\`。

### Phase 2：财务数据拉取（mx-data）

**核心改进：串行查询 + 重试机制，避免并发触发 NoneType 错误。**

对 Phase 1.7 的候选股票（最多 8 只），用 mx-data 拉取：

**查询 1 — 三年年报趋势：**
```bash
cd "C:\Users\69050\.workbuddy\skills\mx-data" && python mx_data.py "{代码} 近三年年报 营收 净利润 毛利率 ROE 资产负债率"
```

**查询 2 — 实时行情 + 均线（合并查询，减少调用次数）：**
```bash
cd "C:\Users\69050\.workbuddy\skills\mx-data" && python mx_data.py "{代码} 最新价 涨跌幅 5日均价 10日均价 20日均价 市盈率 市净率"
```

**⚠️ 并发控制（v2.1 关键修复）：**
- mx-data **不可并发调用**，超过 2 个同时查询会触发 `NoneType` 错误
- 必须**逐个串行执行**，每次查询间隔 1-2 秒
- 如果某次查询失败，**重试 1 次**，仍失败则跳过该股票
- 查询 2（季报）已合并到实时行情查询中，减少总调用次数
- 总调用量：8 只 × 2 查询 = 16 次（串行约需 2-3 分钟）

**Phase 2 输出：** 每只股票的 3 年年报 + 实时行情（含均线）。

### Phase 3：Berkshire 六关快筛 + 技术面验证

**核心改进：增加量化淘汰线 + 技术面确认。**

#### 六关快筛（量化标准）

| 关卡 | 淘汰条件 | 量化阈值 | 说明 |
|------|---------|----------|------|
| 1. 能力圈 | 无法用一句话说清生意本质 | — | 淘汰看不懂的 |
| 2. 好生意 | 毛利率<15% 或连续下滑 | 年报毛利率 < 15% | 淘汰烂生意 |
| 3. 护城河 | ROE<10% 且无改善 | 年报 ROE < 10% | 淘汰无壁垒 |
| 4. 管理层 | 负债率>75% 或频繁减持 | 资产负债率 > 75% | 淘汰高风险 |
| 5. 安全边际 | PE>30 且 PB>5 | PE > 30 或 PB > 5 | 淘汰太贵 |
| 6. 镜子测试 | 5 句话说不清买入逻辑 | — | 淘汰说不清 |

**补充淘汰规则（v2 新增）：**
- **扣非净利润为负**：如果最近一年扣非净利润 < 0，即使归母净利润为正也淘汰（排除靠非经常性损益粉饰的公司）
- **ROE 趋势恶化**：如果最近一期季报 ROE < 年报 ROE 的 50%，标记为"ROE 恶化预警"
- **营收连续下滑**：如果最近 2 年营收都在下滑，标记为"成长性存疑"

#### 技术面确认（v2 新增）

对通过六关的股票，用 Phase 2 查询 3 的均线数据做技术面确认：

| 条件 | 通过标准 | 说明 |
|------|----------|------|
| 趋势 | 最新价 > 20日均价 | 在上升趋势中 |
| 短期动量 | 5日均价 > 10日均价 | 短期均线多头排列 |
| 不追高 | 当日涨幅 < 7% | 避免追涨 |

**注意**：技术面确认是加分项，不是硬性淘汰条件。如果基本面很强但技术面不佳，仍可保留但标注"等待回调"。

**Phase 3 输出：** 通过快筛的 3-5 只股票，附带每只的六关评分 + 技术面状态。

### Phase 4：输出操作建议

将分析结果整理为结构化操作建议，写入当日跟踪文件。

**输出格式：**

```markdown
## 早盘扫描 HH:MM（v2 多策略）

**市场风格**：{自适应判断}
**扫描策略**：A 动量稳健 / B 价值质量 / C 高股息防御
**候选数量**：{N} 只 → 行业筛选 {M} 只 → 快筛通过 {K} 只

### 自选股行情
| 代码 | 名称 | 当前价 | 涨跌幅 | 持仓状态 | 操作建议 |

### 新发现候选
| 代码 | 名称 | 当前价 | PE | ROE | 毛利率 | 涨跌幅 | 行业 | 来源策略 | 快筛结果 |

### 快筛详情
（每只通过的股票列出六关评分 + 技术面状态 + 关键财务数据趋势）

### 淘汰说明
| 代码 | 名称 | 淘汰原因 | 淘汰关卡 |

### 待执行操作（13:30确认）
- 卖出/撤单：{列表}
- 买入候选：{列表，含仓位比例和理由}
```

### Phase 5：更新状态文件

将扫描结果写入 `stock-sim-state.json`（标记 pending 操作），不执行买卖。

**状态文件字段说明：**
- `pending_operations[].status`：
  - `pending_cancel` — 待撤单
  - `pending_confirm` — 待 13:30 确认买入
  - `confirmed` — 已确认，待执行
  - `executed` — 已执行
  - `cancelled` — 已取消

## 30 天模拟跟踪规则

1. **每日触发**：自动定时运行（11:30 / 13:30 / 14:20）
2. **模拟买入**：13:30 确认后以当时价模拟买入（固定仓位 10%-15%）
3. **模拟持有**：买入后持有，除非触发卖出信号
4. **卖出条件**：
   - 建议变为"卖出"或"回避"
   - 持有超过 15 个交易日
   - 亏损超过 10%（止损）
   - 盈利超过 25%（止盈）
5. **记录格式**：每日追加到 `D:\claudecode\.workbuddy\memory\stock-sim-YYYY-MM-DD.md`
6. **状态文件**：`D:\claudecode\.workbuddy\memory\stock-sim-state.json`
7. **30 天后复盘**：统计胜率、盈亏比、与沪深 300 对比

## 资金管理规则

- 总资金：1,000,000（2026-07-01 启动，初始 100 万）
- 单只仓位上限：15%（150,000）
- 最大同时持仓：8 只
- 现金储备下限：20%（200,000）
- 同行业持仓上限：2 只

## 注意事项

- 本 skill 是**投顾建议**，不是量化交易信号
- 所有建议需人工确认后再执行实际操作（或 13:30 自动确认）
- 30 天模拟期间不调整策略参数，保持一致性
- mx-xuangu 和 mx-data 共享同一个 MX_APIKEY 调用配额，注意控制查询次数
- 如果某组策略返回 0 结果，跳过该策略，用其他策略的结果继续
- 如果 3 组策略都返回 0 结果，记录"今日无候选"，不强制选股
- **CSV 输出路径**：mx-xuangu/mx-data 输出到 `C:\root\.openclaw\workspace\mx_data\output\`（Windows 路径）

## 变更日志

### v2.1.0（2026-07-03）
- **策略C 降噪**：新增 ROE>15% 约束 + 市值门槛从 >100亿 提升到 >200亿，解决 200 只噪音问题
- **mx-data 并发修复**：从并发改为串行执行 + 失败重试 1 次，解决 NoneType 错误
- **CSV 数据复用**：新增 Phase 1.7，先从 xuangu CSV 提取 PE/PB/ROE 做粗筛，减少 mx-data 调用量
- **查询合并**：季报查询合并到实时行情查询，Phase 2 从 3 次/只降为 2 次/只
- **资金修正**：总资金从 200,000 修正为 1,000,000（与实际 state.json 一致）
- **输出路径文档化**：记录 CSV 容器路径到 Windows 路径的映射
